# A3C numpy hızlı sim — MINIMAL bağımlılıklar (ROS/Gazebo YOK, MacBook'ta çalışır)
# Kurulum:  python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements_numpy.txt
gymnasium>=0.29
stable-baselines3>=2.3
torch>=2.2
numpy>=1.26
matplotlib>=3.7
pyyaml>=6.0
tqdm>=4.66
rich>=13.0
tensorboard>=2.15
